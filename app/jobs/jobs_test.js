'use strict';

describe('myJobs.jobs module', function() {

  beforeEach(module('myJobs.jobs'));

  describe('Jobs controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('JobsCtrl');
      expect(JobsCtrl).toBeDefined();
    }));

  });
});